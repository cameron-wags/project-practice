{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/practice-api/practice-api/practice-api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}